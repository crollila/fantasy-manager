"""Local-first fantasy football intelligence."""

# Single source of truth for the running application version. Keep in step with
# desktop/package.json (tests/test_api.py asserts the two agree).
__version__ = "0.6.3"
